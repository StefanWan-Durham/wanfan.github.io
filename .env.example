# Copy to .env (do not commit .env). Only one provider is required.
# Optionally set PREFERRED_PROVIDER to choose which to use when multiple are set.

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# OpenRouter
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini

# Together
TOGETHER_API_KEY=
TOGETHER_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct-Turbo

# DeepSeek
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# Optional cap for provider output tokens (overrides LLM_MAX_TOKENS if set)
# DEEPSEEK_MAX_OUTPUT=4096

# DashScope (Qwen OpenAI-compatible)
DASHSCOPE_API_KEY=
DASHSCOPE_MODEL=qwen2.5-72b-instruct
# Optional: DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# General
PREFERRED_PROVIDER=
# Optional global caps
LLM_MAX_TOKENS=4096
